// Central API configuration
// In development, the Vite proxy forwards /api requests to the backend.
// In production (Netlify), the _redirects or netlify.toml proxy handles forwarding.
export const API_URL = "";
